#!/bin/bash
# hk-theme-list
# List installed themes, formatted as "Title Case".

find "$HYPRKARL_PATH/themes/" -mindepth 1 -maxdepth 1 -type d -printf '%f\n' \
  | sort -u \
  | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g'
