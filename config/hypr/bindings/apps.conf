$terminal = uwsm app -- xdg-terminal-exec 2>>/tmp/terminal-launch.log # Currently saving logs for terminal launches due to some inconsistent behavior with ghostty
$terminal-alt = uwsm app -- foot
$browser = hyprkarl-launch-browser

bindd = SUPER, RETURN, Terminal, exec, $terminal --dir="$(hyprkarl-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Alternative terminal, exec, $terminal-alt
bindd = SUPER SHIFT, F, File manager, exec, hyprkarl-launch-tui yazi
bindd = SUPER ALT, F, Alternative file manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER SHIFT, B, Browser, exec, $browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, $browser --private
bindd = SUPER SHIFT, E, Editor, exec, hyprkarl-launch-editor
bindd = SUPER SHIFT, O, Obsidian, exec, hyprkarl-launch-or-focus obsidian "uwsm app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, V, Vesktop, exec, hyprkarl-launch-or-focus vesktop "uwsm app -- vesktop"

bindd = SUPER SHIFT, D, Dictionary, exec, $terminal --app-id=TUI.float -e hyprkarl-dictionary