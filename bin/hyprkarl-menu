#!/bin/bash

LAUNCH="󱓟 Launch"
INSTALL=" Install"
UNINSTALL=" Uninstall"
UTILS=" Utilities"
CONFIG=" Config"
POWER=" Power"

CHOICE=$(echo -e "$LAUNCH\n$INSTALL\n$UNINSTALL\n$UTILS\n$CONFIG\n$POWER" \
    | rofi -dmenu \
           -p "" \
           -no-custom \
           -mesg "Main Menu" \
           -theme ~/.config/rofi/custom-menu.rasi) \
           || pkill rofi

case "$CHOICE" in
    "$LAUNCH")    hyprkarl-menu-launcher ;;
    "$INSTALL")   hyprkarl-menu-install ;;
    "$UNINSTALL") hyprkarl-menu-uninstall ;;
    "$UTILS")     hyprkarl-menu-utils ;;
    "$CONFIG")    hyprkarl-menu-config ;;
    "$POWER")     hyprkarl-menu-power ;;
esac