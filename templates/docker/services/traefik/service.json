{
  "id": "traefik",
  "label": "Traefik",
  "compose_file": "docker-compose.yml",
  "require_traefik_network": true,
  "install_message": "Traefik is running and will automatically route .local hostnames.",
  "remove_warning": "This will remove .local hostnames for other containers. Services will still be accessible via localhost:PORT.",
  "template_files": [
    "docker-compose.yml:docker-compose.yml"
  ]
}
