#!/bin/bash
# hk-launch-bluetooth
# Launch bluetooth controls (provided by bluetui).
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec hk-launch-or-focus-tui bluetui
