# Close windows
bindd = SUPER, W, Close window, killactive,
bindd = SUPER SHIFT, W, Force kill window, forcekillactive

# Identify active window
bindd = SUPER, I, Identify active window, exec, hyprkarl-notify-window-class

# Menus
bindd = SUPER ALT, SPACE, Main menu, exec, hyprkarl-menu || pkill rofi
bindd = SUPER, SPACE, Launch apps, exec, hyprkarl-menu-launcher || pkill rofi
bindd = SUPER, ESCAPE, Power menu, exec, hyprkarl-menu-power || pkill rofi
bindd = SUPER, K, View keybinds, exec, hyprkarl-menu-keybindings || pkill rofi
bindd = SUPER CTRL, SPACE, Wallpaper menu, exec, hyprkarl-menu-wallpaper || pkill rofi

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, makoctl mode -t do-not-disturb && makoctl mode | grep -q 'do-not-disturb' && notify-send "Silenced notifications" || notify-send "Enabled notifications"
bindd = SUPER ALT, COMMA, Invoke last notification, exec, makoctl invoke
bindd = SUPER SHIFT ALT, COMMA, Restore last notification, exec, makoctl restore

# Toggle nightlight
bindd = SUPER CTRL, N, Toggle nightlight, exec, hyprkarl-nightlight

# Control panels
bindd = SUPER CTRL, A, Audio controls, exec, hyprkarl-launch-audio
bindd = SUPER CTRL, B, Bluetooth controls, exec, hyprkarl-launch-bluetooth
bindd = SUPER CTRL, W, Wifi controls, exec, hyprkarl-launch-wifi
bindd = SUPER CTRL, T, Activity, exec, hyprkarl-launch-tui btop

# Screenshots
bindd = SUPER, PRINT, Screenshot window, exec, hyprshot -m window
bindd = , PRINT, Screenshot display, exec, hyprshot -m output
bindd = SHIFT, PRINT, Screenshot region, exec, hyprshot -m region

# Suspend on lid close
bindl = , switch:on:Lid Switch, exec, hyprkarl-suspend

# Open power menu with the power button
bind = , XF86PowerOff, exec, hyprkarl-menu-power