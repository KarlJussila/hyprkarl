#!/bin/bash
# hk-menu-install
# Menu for installing packages

ROFI_THEME="${XDG_CONFIG_HOME:-$HOME/.config}/rofi/custom-menu.rasi"

PACMAN="󰣇 Pacman"
AUR=" AUR"
FLATPAK="󰏖 Flatpak"
DOCKER="󰡨 Docker"

relaunch_menu() { exec "$0"; }

CHOICE=$(echo -e "$PACMAN\n$AUR\n$FLATPAK\n$DOCKER" \
    | rofi -dmenu \
           -p "" \
           -no-custom \
           -mesg "Install" \
           -theme "$ROFI_THEME")

case "$CHOICE" in
    "$PACMAN")  hk-open-terminal hk-pkg-install-tui ;;
    "$AUR")     hk-open-terminal hk-pkg-install-tui --aur ;;
    "$FLATPAK") hk-open-terminal hk-pkg-install-tui --flatpak ;;
    "$DOCKER")  hk-menu-docker-install || relaunch_menu ;;
    "")         exit 1 ;;
esac

exit 0
