#!/bin/bash

# hyprkarl-fingerprint-list
# Lists enrolled fingerprints. Outputs one finger name per line.

# Usage:
#   hyprkarl-fingerprint-list

if ! command -v fprintd-list &>/dev/null; then
  exit 1
fi

fprintd-list "$USER" 2>/dev/null | awk -F': ' '/^ - #/ {print $2}'